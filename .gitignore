# ToS: never commit raw teacher outputs (D1)
*_gold_raw.jsonl
data/teacher_outputs/
# credentials
*.key
*api*key*
.env
# student weights trained on teacher outputs (D3)
legal-code-*-v1/
legal-code-union-*/
*_ckpt/
# API key leak prevention (Gemini etc.)
gemini.txt
gemini.key
*.key
.genesis_secrets/
**/gemini.txt
*token*
*secret*

# third-party copyrighted references (never commit)
**/references/
**/REFERENCE_ONLY_*

# build / run caches (do not commit)
__pycache__/
*.pyc
*.pyo
unsloth_compiled_cache/
.venv/
venv/
*.egg-info/
